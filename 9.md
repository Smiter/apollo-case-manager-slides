# Graphql schema mocks > individual requests mocks

- It scales better
- Less friction
- Mocking schema fields way easier than individual API requests
- Faster feedback loop
- The fastest way I’ve found to write + iterate on your frontend code
